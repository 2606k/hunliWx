<!--pages/index/index.wxml-->
<view class="container" bindlongpress="onLongPress">
  <!-- 背景音乐组件 -->
  <bgm></bgm>
  
  <!-- 蓝色按钮 - 绝对定位在指定区域 -->
  <view class="blue-button {{debugMode ? 'debug-border' : ''}}" bindtap="onBlueButtonClick">
    <text wx:if="{{debugMode}}" class="debug-text">故事</text>
  </view>
  <view class="blue-button2 {{debugMode ? 'debug-border' : ''}}" bindtap="onBlueButtonClick2">
    <text wx:if="{{debugMode}}" class="debug-text">相册</text>
  </view>
  <view class="blue-button3 {{debugMode ? 'debug-border' : ''}}" bindtap="onBlueButtonClick3">
    <text wx:if="{{debugMode}}" class="debug-text">留言</text>
  </view>
  
  <!-- <view class="backImg">
    <image src="https://marry-wx.oss-cn-beijing.aliyuncs.com/indexPhoto.jpg"></image>
  </view> -->
  <!-- 装饰元素 -->
  <view class="decoration-hearts">
    <text class="heart">💖</text>
    <text class="heart">💕</text>
    <text class="heart">💗</text>
    <text class="heart">💝</text>
    <text class="heart">💖</text>
  </view>

  <!-- 主要内容 -->
  <view class="main-content">
    <!-- 移除了用户授权区域和已授权用户功能区域 -->
  </view>
</view> 