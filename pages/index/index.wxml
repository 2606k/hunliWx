<!--pages/index/index.wxml-->
<view class="container">
  <!-- 背景音乐组件 -->
  <bgm></bgm>
  
  <!-- 蓝色按钮 - 绝对定位在指定区域 -->
  <view class="blue-button" bindtap="onBlueButtonClick"></view>
  <view class="blue-button2" bindtap="onBlueButtonClick2"></view>
  <view class="blue-button3" bindtap="onBlueButtonClick3"></view>
  
  <!-- <view class="backImg">
    <image src="https://marry-wx.oss-cn-beijing.aliyuncs.com/indexPhoto.jpg"></image>
  </view> -->
  <!-- 装饰元素 -->
  <view class="decoration-hearts">
    <text class="heart">💖</text>
    <text class="heart">💕</text>
    <text class="heart">💗</text>
    <text class="heart">💝</text>
    <text class="heart">💖</text>
  </view>

  <!-- 主要内容 -->
  <view class="main-content">
    <!-- 新人信息 -->
    <!-- <view class="couple-info">
      <view class="names">
        <text class="name">{{weddingInfo.brideName}}</text>
        <text class="and">&</text>
        <text class="name">{{weddingInfo.groomName}}</text>
      </view>
      <view class="wedding-date">{{formattedDate}}</view>
      <view class="location">{{weddingInfo.weddingLocation}}</view>
    </view> -->

    <!-- 倒计时区域 - 对所有用户显示 -->
    <!-- <view class="countdown-section">
      <view class="countdown-title">距离婚礼还有</view>
      <view class="countdown-time">
        <view class="time-item">
          <text class="time-number">{{countdown.days || 0}}</text>
          <text class="time-label">天</text>
        </view>
        <view class="time-item">
          <text class="time-number">{{countdown.hours || 0}}</text>
          <text class="time-label">时</text>
        </view>
        <view class="time-item">
          <text class="time-number">{{countdown.minutes || 0}}</text>
          <text class="time-label">分</text>
        </view>
        <view class="time-item">
          <text class="time-number">{{countdown.seconds || 0}}</text>
          <text class="time-label">秒</text>
        </view>
      </view>
    </view> -->

    <!-- 用户授权区域 - 改为底部小按钮 -->
    <view class="auth-bottom-section" wx:if="{{!hasUserInfo}}">
      <button class="btn-auth-small" bindtap="getUserProfile">
        <text class="auth-btn-text">授权登录</text>
      </button>
      <view class="auth-tips-small">授权后可留言祝福、查看更多功能</view>
    </view>

    <!-- 已授权用户功能区域 -->
    <view class="feature-section" wx:if="{{hasUserInfo}}">
      <!-- 核心功能 -->
      <!-- <view class="core-features">
        <view class="feature-item" bindtap="goToMessage">
          <view class="feature-icon">💝</view>
          <view class="feature-content">
            <view class="feature-title">留言祝福</view>
            <view class="feature-desc">为新人送上您的祝福</view>
          </view>
          <view class="feature-arrow">></view>
        </view>
        
        <view class="feature-item" bindtap="goToPhoto">
          <view class="feature-icon">📸</view>
          <view class="feature-content">
            <view class="feature-title">相册浏览</view>
            <view class="feature-desc">查看婚礼精彩瞬间</view>
          </view>
          <view class="feature-arrow">></view>
        </view>
      </view> -->

      <!-- 用户信息展示 -->
      <!-- <view class="user-info-section">
        <view class="user-avatar">
          <image src="{{userInfo.avatarUrl}}" mode="aspectFill" class="avatar-image"></image>
        </view>
        <view class="user-details">
          <view class="user-name">{{userInfo.nickName}}</view>
          <view class="user-status">已授权 · 欢迎您</view>
        </view>
      </view> -->
    </view>
  </view>
</view> 